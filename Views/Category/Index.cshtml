@{
    ViewBag.Title = "Kategoriler";
    var categories = ViewData.Model as List<StockifyPlus.Models.Category> ?? new List<StockifyPlus.Models.Category>();
}

<div class="sp-page-shell">
<div class="sp-page-header">
    <h2 class="sp-page-title">
        <i class="fas fa-tags"></i> Kategori Yönetimi
    </h2>
    <div class="sp-toolbar">
        <a href="@Url.Action("Create", "Category")" class="btn btn-primary btn-custom">
            <i class="fas fa-plus"></i> Yeni Kategori
        </a>
    </div>
</div>

@if (categories.Any())
{
    <div class="table-responsive sp-table-wrap">
        <table class="table table-hover sp-table">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Kategori Adı</th>
                    <th>Açıklama</th>
                    <th>Ürün Sayısı</th>
                    <th>Durum</th>
                    <th>İşlemler</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var category in categories)
                {
                    <tr>
                        <td>@category.Id</td>
                        <td><strong>@category.Name</strong></td>
                        <td>@(string.IsNullOrEmpty(category.Description) ? "-" : category.Description)</td>
                        <td>
                            <span class="badge bg-info">@(category.Products?.Count(p => p.IsActive) ?? 0) ürün</span>
                        </td>
                        <td>
                            @if (category.IsActive)
                            {
                                <span class="badge bg-success">Aktif</span>
                            }
                            else
                            {
                                <span class="badge bg-secondary">Pasif</span>
                            }
                        </td>
                        <td>
                            <div class="sp-action-cluster">
                            <a href="@Url.Action("Edit", "Category", new { id = category.Id })"
                               class="btn btn-sm btn-warning btn-custom" title="Düzenle" aria-label="@category.Name düzenle">
                                <i class="fas fa-edit"></i>
                            </a>
                            <a href="@Url.Action("Delete", "Category", new { id = category.Id })"
                               class="btn btn-sm btn-danger btn-custom" title="Sil" aria-label="@category.Name sil">
                                <i class="fas fa-trash"></i>
                            </a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
}
else
{
    <div class="sp-empty-state">
        <div>
            <i class="fas fa-tags"></i>
            <div>Henüz kategori oluşturulmamış.</div>
            <a href="@Url.Action("Create", "Category")">Yeni kategori oluşturmak için tıklayın.</a>
        </div>
    </div>
}
</div>
